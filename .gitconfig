#
# ~/.gitconfig
#

[core]
	excludesfile = ~/.cvsignore
	pager = less -eFRSX

[alias]
	ap = add --patch
	br = branch
	ca = commit --amend
	ci = commit
	co = checkout
	dc = diff --cached
	di = diff
	st = status --short --branch

[diff]
	mnemonicprefix = true

[grep]
	patterntype = perl

[merge]
	tool = vimdiff

[mergetool]
	prompt = false

[push]
	default = current

[include]
	path = ~/.gitconfig.local
