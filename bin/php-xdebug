#!/bin/sh

php=${PHP_BINARY:-php}

if ! "$php" -m | grep -Fxq xdebug
then
    set -- -d "zend_extension=${XDEBUG_EXTENSION:-xdebug.so}" "$@"
fi

XDEBUG_CONFIG=${XDEBUG_CONFIG-remote_enable=1} exec "$php" "$@"
