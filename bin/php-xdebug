#!/bin/sh

: "${PHP_BINARY:=php}"

if ! "$PHP_BINARY" -m | grep -Fxq xdebug
then
    set -- -d zend_extension="${PHP_ZEND_EXTENSION_XDEBUG:-xdebug.so}" "$@"
fi

: "${XDEBUG_CONFIG:=remote_enable=1}"
export XDEBUG_CONFIG

exec "$PHP_BINARY" "$@"
